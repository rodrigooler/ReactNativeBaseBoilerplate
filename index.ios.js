/**
 * Sample React NativeBase App
 * https://github.com/RodrigoOler/ReactNativeBoilerplate
 * @flow
 */

import {AppRegistry} from 'react-native';
import ReactNativeBaseBoilerplate from './src';

AppRegistry.registerComponent('ReactNativeBaseBoilerplate', () => ReactNativeBaseBoilerplate);
